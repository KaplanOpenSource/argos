import { Path } from "leaflet";

export const DEFAULT_COLOR = new Path().options.color;
export const DEFAULT_FILL_OPACITY = new Path().options.fillOpacity;
export const DEFAULT_LINE_OPACITY = new Path().options.opacity;

